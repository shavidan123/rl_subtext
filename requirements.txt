# Fruit Signaling Experiment Dependencies

# Core ML - PyTorch with CUDA support
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# Transformers ecosystem
transformers>=4.35.0
peft>=0.7.0
bitsandbytes>=0.41.0
accelerate>=0.24.0

# RL training
trl>=0.9.0

# Unsloth for 2x faster training
unsloth

# API calls for GPT-4o monitor
requests>=2.28.0

# Config and utilities
pyyaml>=6.0
python-dotenv>=1.0.0

# Progress and logging
tqdm>=4.65.0
